meeting-log-mailer

A project to mail meeting logs generated by MeetBot. This may end up not working at all and being scrapped. We'll see. I probably should have just set up a local repo initially instead of creating a github repo for it, but hey...those are cheap, so why not?
